apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: petpal
  labels:
    app: appointment-service
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
        version: v1
    spec:
      containers:
      - name: appointment-service
        image: gcr.io/petpal-467223/appointment-service:latest
        ports:
        - containerPort: 5003
          name: http
        env:
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: petpal-secrets
              key: mongo-uri
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: petpal-secrets
              key: secret-key
        - name: PORT
          value: "5003"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: petpal
  labels:
    app: appointment-service
spec:
  selector:
    app: appointment-service
  ports:
  - port: 5003
    targetPort: 5003
    protocol: TCP
    name: http
  type: ClusterIP