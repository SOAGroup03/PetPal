apiVersion: apps/v1
kind: Deployment
metadata:
  name: pet-service
  namespace: petpal
  labels:
    app: pet-service
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pet-service
  template:
    metadata:
      labels:
        app: pet-service
        version: v1
    spec:
      containers:
      - name: pet-service
        image: gcr.io/petpal-467223/pet-service:latest
        ports:
        - containerPort: 5002
          name: http
        env:
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: petpal-secrets
              key: mongo-uri
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: petpal-secrets
              key: secret-key
        - name: PORT
          value: "5002"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5002
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: pet-service
  namespace: petpal
  labels:
    app: pet-service
spec:
  selector:
    app: pet-service
  ports:
  - port: 5002
    targetPort: 5002
    protocol: TCP
    name: http
  type: ClusterIP