apiVersion: apps/v1
kind: Deployment
metadata:
  name: medical-service
  namespace: petpal
  labels:
    app: medical-service
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: medical-service
  template:
    metadata:
      labels:
        app: medical-service
        version: v1
    spec:
      containers:
      - name: medical-service
        image: gcr.io/petpal-467223/medical-service:latest
        ports:
        - containerPort: 5004
          name: http
        env:
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: petpal-secrets
              key: mongo-uri
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: petpal-secrets
              key: secret-key
        - name: PORT
          value: "5004"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5004
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: medical-service
  namespace: petpal
  labels:
    app: medical-service
spec:
  selector:
    app: medical-service
  ports:
  - port: 5004
    targetPort: 5004
    protocol: TCP
    name: http
  type: ClusterIP